# Технический бэклог переработки frontend

## Сайт о творчестве Ивана Кугача

Версия документа: 1.0  
Дата формирования: 2026-06-19  
Назначение: рабочий бэклог перехода от single-file прототипа к структурированному многостраничному frontend-проекту  
Текущий источник поведения: `index_masterskaya.html`  
Целевой стек: Astro + TypeScript + CSS + изолированные клиентские модули интерактивов

---

## 1. Цель переработки

Перевести текущий proof-of-concept из одного HTML-файла в полноценный frontend-проект, который:

- имеет настоящие страницы и стабильные URL;
- собирается предсказуемой production-командой;
- разделяет разметку, стили, данные и интерактивную логику;
- сохраняет существующий визуальный образ мастерской и работающие сценарии;
- позволяет развивать каталог и контент без редактирования большого HTML-файла;
- подготовлен к последующему подключению CMS и backend, но пока от них не зависит;
- имеет автоматические проверки и понятный процесс ручного QA.

Ключевой принцип миграции: не переписывать весь прототип одним большим изменением. Каркас, страницы и интерактивы переносятся последовательно, а текущий `index_masterskaya.html` сохраняется как рабочий визуальный и функциональный эталон до завершения миграции.

---

## 2. Что входит в этот бэклог

- создание Astro-проекта;
- TypeScript и структура исходников;
- production-сборка и локальный preview;
- маршруты и настоящие страницы;
- общие layout и компоненты;
- перенос визуальной системы мастерской;
- перенос narrative-маршрута;
- перенос трёх интерактивов;
- модель данных каталога, серий и сюжетных материалов;
- frontend-форма заявки без реальной отправки;
- адаптивность и доступность;
- оптимизация загрузки изображений;
- SEO-основа на уровне frontend;
- автоматические проверки;
- техническая документация проекта.

## 3. Что пока не входит

- выбор домена и хостинга;
- production-деплой;
- Strapi или другая CMS;
- база данных;
- настоящая отправка заявок;
- email, Telegram, CRM и другие интеграции;
- авторизация сотрудников;
- онлайн-оплата;
- автоматическое резервирование;
- юридическое утверждение текстов;
- аналитика и cookie-consent;
- многоязычность.

При этом frontend должен быть устроен так, чтобы данные будущей CMS можно было подключить вместо локальных данных без повторной переделки страниц и компонентов.

---

## 4. Текущее техническое состояние

Актуальный прототип находится в `index_masterskaya.html`.

На момент аудита:

- один HTML-файл содержит более 5 200 строк;
- CSS занимает более 2 400 строк внутри `<style>`;
- JavaScript занимает более 2 100 строк внутри `<script>`;
- все экраны находятся в одном DOM и переключаются через класс `hidden`;
- адрес страницы не меняется при переходе между разделами;
- каталог работ, narrative-последовательности и hotspots хранятся в JS-константах;
- в коде зарегистрировано около 70 обработчиков событий;
- формы валидируются только в браузере и имитируют успешную подготовку заявки;
- изображения в `src/` занимают около 25,6 МБ;
- сборщик, package manager scripts, TypeScript и автоматические тесты отсутствуют;
- синтаксис текущего встроенного JavaScript корректен;
- все используемые локальные изображения существуют;
- дублирующихся HTML `id` не обнаружено.

Текущее состояние приемлемо как proof-of-concept, но создаёт риски при развитии: глобальное состояние, сильная связанность экранов, отсутствие маршрутов, тяжёлые изображения и невозможность независимо проверять части приложения.

---

## 5. Целевая архитектура

Предлагаемая структура:

```text
.
├── astro.config.mjs
├── package.json
├── public/
│   ├── favicon.svg
│   └── ...
├── src/
│   ├── assets/
│   │   ├── artworks/
│   │   ├── interactive/
│   │   └── studio/
│   ├── components/
│   │   ├── common/
│   │   ├── artwork/
│   │   ├── narrative/
│   │   └── interactive/
│   ├── content/
│   │   ├── artworks/
│   │   ├── series/
│   │   └── narratives/
│   ├── data/
│   │   ├── light-works.ts
│   │   └── hotspots.ts
│   ├── layouts/
│   │   └── BaseLayout.astro
│   ├── pages/
│   │   ├── index.astro
│   │   ├── artist.astro
│   │   ├── works/
│   │   │   ├── index.astro
│   │   │   └── [slug].astro
│   │   ├── series/
│   │   │   └── [slug].astro
│   │   ├── archive.astro
│   │   └── experience/
│   │       ├── index.astro
│   │       ├── color-return.astro
│   │       ├── details.astro
│   │       └── light.astro
│   ├── scripts/
│   │   ├── color-return/
│   │   ├── details-explorer/
│   │   └── light-workshop/
│   ├── styles/
│   │   ├── tokens.css
│   │   ├── global.css
│   │   ├── studio.css
│   │   └── utilities.css
│   ├── types/
│   │   └── content.ts
│   └── utils/
├── tests/
└── docs/
```

Целевая карта страниц первой технической версии:

| URL                         | Назначение                                  |
| --------------------------- | ------------------------------------------- |
| `/`                         | Главная мастерской                          |
| `/artist/`                  | Страница художника                          |
| `/experience/`              | Вход в интерактивный маршрут                |
| `/experience/color-return/` | Интерактив «Возвращение цвета»              |
| `/experience/details/`      | Интерактив «Приблизить детали»              |
| `/experience/light/`        | Интерактив света                            |
| `/works/`                   | Каталог доступных и зарезервированных работ |
| `/works/[slug]/`            | Страница отдельной работы                   |
| `/series/[slug]/`           | Страница серии                              |
| `/archive/`                 | Архив проданных работ                       |
| `/404.html`                 | Страница ненайденного маршрута              |

---

## 6. Логика технических версий

### Frontend v0.1. Каркас и безопасная миграция

Цель: создать запускаемый Astro-проект рядом с текущим прототипом, не ломая эталон.

Результат:

- проект запускается через `npm.cmd run dev`;
- production-сборка создаётся через `npm.cmd run build`;
- preview запускается через `npm.cmd run preview`;
- есть базовый layout, глобальные стили и пустые маршруты;
- текущий прототип продолжает открываться отдельно.

### Frontend v0.2. Многостраничная оболочка

Цель: перенести обычные контентные разделы и навигацию на настоящие маршруты.

Результат:

- есть главная, страница художника, каталог, архив и страницы работ;
- навигация работает обычными ссылками;
- обновление страницы и прямой вход по URL не ломают интерфейс;
- данные каталога не находятся внутри компонентов.

### Frontend v0.3. Интерактивный маршрут

Цель: перенести narrative-систему и три интерактива без потери поведения.

Результат:

- каждый интерактив изолирован в собственном модуле;
- интерактивы не загружают код друг друга;
- маршрут между ними работает через URL и явные переходы;
- прогресс активного интерактива хранится только локально в его модуле.

### Frontend v0.4. Качество интерфейса

Цель: довести адаптивность, доступность и загрузку ресурсов до уровня полноценного сайта.

Результат:

- оптимизированы изображения;
- пройдены целевые desktop, tablet и mobile viewport;
- модальные окна и управление доступны с клавиатуры;
- отсутствует горизонтальное переполнение;
- основные страницы имеют корректные метаданные.

### Frontend v1.0. Готовый frontend

Цель: получить поддерживаемую production-сборку, готовую к отдельному этапу интеграции с CMS и backend.

Результат:

- все актуальные сценарии перенесены;
- single-file прототип больше не является точкой входа;
- локальная модель данных соответствует будущему CMS-контракту;
- проверки выполняются одной командой;
- есть документация по структуре, контенту и ручному QA.

---

## 7. Правила ведения бэклога

Статусы:

- `[ ]` — не начато;
- `[~]` — в работе;
- `[x]` — завершено;
- `[!]` — заблокировано.

Приоритеты:

- **Must** — обязательно для Frontend v1.0;
- **Should** — желательно выполнить до Frontend v1.0;
- **Could** — можно отложить без разрушения архитектуры.

Задача считается завершённой только после выполнения её критериев готовности и указанной проверки. Простого наличия кода недостаточно.

---

## 8. Эпики и задачи

## EPIC F1. Основа проекта и инструменты

### [x] FRT-001. Зафиксировать архитектурные решения

Приоритет: Must  
Версия: Frontend v0.1

Описание:

- зафиксировать Astro как основу многостраничного frontend;
- использовать TypeScript в строгом режиме;
- оставить интерактивы на DOM/Canvas API без обязательного React/Vue;
- использовать Astro Content Collections или типизированные локальные данные;
- зафиксировать границы frontend-заглушки формы;
- описать правило: CMS в будущем заменяет источник данных, а не структуру UI.

Критерии готовности:

- решения записаны в коротком architecture-разделе README или отдельном документе;
- для каждого решения понятна причина;
- отсутствуют две конкурирующие структуры проекта.

Проверка:

- разработчик может по документации объяснить, где находятся страницы, компоненты, данные и интерактивная логика.

Выполнено 2026-06-19:

- в `README.md` зафиксированы Astro, строгий TypeScript и DOM/Canvas-модули без обязательного React/Vue;
- описаны единая целевая структура, роли директорий, локальные источники данных и граница frontend-заглушки формы;
- закреплено правило: будущая CMS заменяет слой источника данных, не меняя компоненты и структуру UI;
- проверить: прочитать раздел `Архитектурные решения frontend` в `README.md` и убедиться, что по таблице можно определить место страниц, компонентов, данных, интерактивной логики, типов и адаптеров.

### [x] FRT-002. Инициализировать Astro + TypeScript

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-001

Описание:

- создать `package.json`;
- подключить Astro;
- включить строгую проверку TypeScript;
- создать `astro.config.mjs`;
- добавить базовые страницы и layout;
- не удалять `index_masterskaya.html`.

Критерии готовности:

- dev-сервер запускается;
- начальная страница открывается без runtime-ошибок;
- старый прототип сохранён и доступен для сравнения.

Проверка:

```powershell
npm.cmd install
npm.cmd run dev
```

Выполнено 2026-06-19:

- добавлены `package.json`, `package-lock.json`, `astro.config.mjs` и строгий `tsconfig.json`;
- созданы минимальные `src/layouts/BaseLayout.astro` и `src/pages/index.astro`;
- legacy-файл `index_masterskaya.html` сохранён без изменений как полнофункциональный эталон;
- Astro 6.4.8 запускается на `http://127.0.0.1:4321/`, стартовая страница отвечает без runtime-ошибок;
- проверить: выполнить `npm.cmd install`, затем `npm.cmd run dev`, открыть выданный локальный URL и увидеть заголовок `Новая версия сайта готовится`.

### [x] FRT-003. Настроить команды проекта

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-002

Описание:

- добавить `dev`;
- добавить `build`;
- добавить `preview`;
- добавить `check`;
- добавить `lint`;
- добавить общую команду `verify`.

Критерии готовности:

- команды работают в PowerShell через `npm.cmd`;
- `verify` последовательно выполняет статические проверки и production-сборку.

Проверка:

```powershell
npm.cmd run check
npm.cmd run lint
npm.cmd run build
npm.cmd run verify
```

Выполнено 2026-06-19:

- в `package.json` добавлены команды `dev`, `build`, `preview`, `check`, `lint` и `verify`;
- подключены официальные `@astrojs/check` и TypeScript;
- `verify` последовательно выполняет `check`, `lint` и production-сборку;
- до FRT-004 команда `lint` использует строгую Astro/TypeScript-проверку и будет заменена полноценным ESLint-контуром;
- проверить в PowerShell: последовательно выполнить команды из блока проверки выше; каждая должна завершиться с кодом `0`.

### [x] FRT-004. Настроить форматирование и статический анализ

Приоритет: Should  
Версия: Frontend v0.1  
Зависимости: FRT-002

Описание:

- настроить Prettier для Astro, TypeScript, CSS и Markdown;
- настроить ESLint;
- исключить generated output и архивные HTML-файлы;
- добавить проверку неиспользуемых импортов и небезопасных `any`.

Критерии готовности:

- новые файлы форматируются единообразно;
- lint не анализирует старые архивные прототипы как production-код.

Выполнено 2026-06-19:

- добавлены flat-конфигурация ESLint, Prettier и официальный `prettier-plugin-astro`;
- ESLint проверяет Astro, TypeScript и корневые JS/MJS-конфиги, запрещает явный `any` и неиспользуемые импорты;
- `index_masterskaya.html`, `archive/`, `experiments/`, generated output и временные директории исключены;
- добавлены команды `format` и `format:check`, а `verify` включает проверку форматирования;
- проверить: выполнить `npm.cmd run lint`, `npm.cmd run format:check` и `npm.cmd run verify`; все команды должны завершиться с кодом `0`.

### [x] FRT-005. Зафиксировать базовую структуру директорий

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-002

Описание:

- создать каталоги `pages`, `layouts`, `components`, `content`, `data`, `scripts`, `styles`, `types`, `utils`;
- определить правила именования компонентов и модулей;
- отделить web-ассеты от документов и экспериментальных файлов.

Критерии готовности:

- каждый тип кода имеет однозначное место;
- новые страницы не создают собственные глобальные стили и данные без необходимости.

Выполнено 2026-06-19:

- созданы `assets`, `components`, `content`, `data`, `scripts`, `styles`, `types`, `utils` и корневой `public`;
- в `src/README.md` зафиксированы ответственность директорий, правила именования и границы страниц;
- legacy-изображения оставлены на прежних путях до отдельной asset-миграции, чтобы не сломать `index_masterskaya.html`;
- `PROJECT_MAP.md` обновлён по фактически существующей структуре;
- проверить: выполнить `Get-ChildItem src, public -Recurse -Force`, прочитать `src/README.md`, затем запустить `npm.cmd run verify`.

---

## EPIC F2. Сохранение эталона и стратегия миграции

### [x] FRT-006. Зафиксировать функциональную карту текущего прототипа

Приоритет: Must  
Версия: Frontend v0.1

Описание:

- перечислить все актуальные экраны;
- перечислить переходы и CTA;
- перечислить модальные окна;
- перечислить состояния трёх интерактивов;
- перечислить элементы каталога и формы;
- отделить актуальную функциональность от экспериментов в `experiments/`.

Критерии готовности:

- существует короткий migration checklist;
- ни один работающий сценарий не переносится «по памяти».

Выполнено 2026-06-19:

- создан `docs/LEGACY_MIGRATION_CHECKLIST.md` с шестью экранами, переходами, CTA и keyboard-сценариями;
- перечислены шесть модальных сценариев, четыре narrative-последовательности и состояния трёх интерактивов;
- зафиксированы 5 работ каталога, статусы, типы цены, две формы и их frontend-валидация;
- `experiments/`, `archive/` и `scrns/` явно отделены от обязательного migration scope;
- проверить: открыть checklist и для выбранного сценария найти соответствующие ID, константы и обработчики в `index_masterskaya.html`.

### [x] FRT-007. Подготовить baseline визуальной проверки

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-006

Описание:

- сохранить скриншоты актуальных ключевых экранов;
- зафиксировать desktop и mobile viewport;
- сохранить состояния открытых модалок и завершённых интерактивов;
- указать допустимые намеренные отличия будущей версии.

Критерии готовности:

- для каждого переносимого экрана есть визуальный ориентир;
- после миграции можно отличить регрессию от осознанной переработки.

Выполнено 2026-06-19:

- сохранены full-page PNG всех 6 экранов в desktop `1440×900` и mobile `390×844`;
- отдельно сохранены 6 desktop-состояний открытых модалок и завершённых интерактивов;
- создан `docs/VISUAL_BASELINE.md` с индексом снимков, обязательным визуальным контрактом и допустимыми намеренными отличиями;
- browser console проверена: runtime-ошибок нет, известен только `404` отсутствующего `/favicon.ico`;
- проверить: открыть `docs/VISUAL_BASELINE.md`, перейти по desktop/mobile ссылкам каждого экрана и сопоставить completion/modal-снимки с checklist FRT-006.

### [x] FRT-008. Определить порядок переключения точки входа

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-002, FRT-006

Описание:

- сохранить `index_masterskaya.html` как временный эталон;
- не переименовывать его в новый `index.html`;
- определить, когда Astro-страница становится основной;
- после завершения миграции переместить legacy-прототип в `archive/`.

Критерии готовности:

- в процессе работ всегда существует одна явно обозначенная новая точка входа и один legacy-эталон;
- нет двух файлов, которые оба считаются production-главной.

Выполнено 2026-06-22:

- создан `docs/ENTRYPOINT_CUTOVER_PLAN.md` со статусами legacy, Astro candidate и generated production entry;
- запрещено переименовывать legacy в корневой `index.html` или редактировать `dist/index.html` вручную;
- переключение разрешено только в FRT-062 после FRT-054, FRT-058–FRT-061 и дополнительных release-gates;
- зафиксированы точный путь архива `archive/index_masterskaya.html`, проверка asset-путей и rollback;
- проверить: по таблицам документа объяснить, какой файл является текущим эталоном, какой — новой candidate-точкой и что изменится только после FRT-062.

---

## EPIC F3. Дизайн-система и общая оболочка

### [x] FRT-009. Вынести дизайн-токены

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-005

Описание:

- перенести цвета, типографику, тени, радиусы, слои и интервалы в `tokens.css`;
- сохранить стилистику мастерской;
- отделить семантические токены от случайных локальных значений;
- предусмотреть токены focus-state и reduced motion.

Критерии готовности:

- основные цвета и размеры не дублируются по компонентам;
- изменение ключевого токена предсказуемо влияет на весь сайт.

Выполнено 2026-06-22:

- создан `src/styles/tokens.css` с базовой палитрой и семантическими токенами цветов;
- вынесены шкалы типографики, интервалов, радиусов, теней и z-index-слоёв;
- добавлены единые focus-ring и motion-токены с переопределением при `prefers-reduced-motion: reduce`;
- токены подключены в `BaseLayout.astro`, поэтому доступны всем страницам общего Astro-layout;
- проверить: изменить `--color-accent` или `--space-6`, запустить Astro и убедиться, что использующие семантический токен элементы получают новое значение без локального дублирования.

### [x] FRT-010. Создать базовый layout

Приоритет: Must  
Версия: Frontend v0.1  
Зависимости: FRT-009

Описание:

- создать `BaseLayout.astro`;
- подключить язык страницы, viewport, title и description;
- подключить глобальные стили;
- добавить skip-link;
- определить слоты для header, main и footer;
- обеспечить единый порядок заголовков.

Критерии готовности:

- все обычные страницы используют один layout;
- страницы не дублируют `<html>`, `<head>` и общие метаданные.

Выполнено 2026-06-22:

- `BaseLayout.astro` расширен общими `lang`, viewport, title и description без дублирования на странице;
- создан `src/styles/global.css`, который подключает токены, reset, базовую типографику и общую сетку страницы;
- добавлены skip-link и фокусируемый `#main-content`;
- определены необязательные named slots `header` и `footer`, default slot используется для единственного main;
- правило одного `<h1>` и последовательных `<h2>`/`<h3>` закреплено в `src/README.md`;
- проверить: открыть `/`, нажать `Tab`, активировать «Перейти к содержанию» и убедиться, что фокус переходит на main; в HTML должны быть один `<html>`, один `<main>` и один `<h1>`.

### [x] FRT-011. Реализовать общую навигацию

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-010

Описание:

- добавить ссылки на главную, художника, интерактивный маршрут и каталог;
- реализовать desktop и mobile состояние;
- визуально отмечать текущий раздел;
- обеспечить keyboard navigation;
- не использовать JS-переключение экранов вместо ссылок.

Критерии готовности:

- каждый основной раздел доступен обычной ссылкой;
- прямой вход и обновление страницы сохраняют правильное состояние навигации.

Выполнено 2026-06-22:

- создан `SiteNavigation.astro` со ссылками на `/`, `/artist/`, `/experience/` и `/works/`;
- текущий раздел определяется из `Astro.url.pathname` и отмечается через `aria-current="page"`;
- desktop-навигация перестраивается в компактную mobile-сетку без JS и скрытых пунктов;
- добавлены минимальные технические страницы целевых разделов, чтобы прямой вход и refresh уже работали до переноса их полного содержимого;
- каталог остаётся активным также для будущих `/series/` и `/archive/`, интерактивный раздел — для вложенных `/experience/*`;
- проверить: открыть каждый из четырёх URL напрямую, обновить страницу и убедиться, что соответствующая ссылка подчёркнута; пройти все ссылки и skip-link клавишами `Tab`.

### [x] FRT-012. Выделить общие UI-компоненты

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-009, FRT-010

Описание:

- кнопки и ссылки-действия;
- section header;
- meta pill/status badge;
- framed artwork;
- карточка запуска интерактива;
- progress indicator;
- modal/dialog shell;
- form field и message state.

Критерии готовности:

- повторяющиеся элементы не копируются между страницами;
- компоненты имеют понятные props и не читают глобальный DOM.

Выполнено 2026-06-22:

- в `src/components/common/` созданы `Action`, `SectionHeader`, `MetaBadge`, `FramedArtwork`, `InteractiveLaunchCard`, `ProgressIndicator`, `DialogShell`, `FormField` и `MessageState`;
- ссылки и кнопки используют один компонент с вариантами, а повторяющаяся разметка технических страниц заменена общими `SectionHeader`, `MessageState` и `Action`;
- поля формы связывают label, hint/error, `aria-invalid` и поддерживают input, textarea и select;
- dialog-shell использует нативный `<dialog>` и доступные title/description; управление фокусом остаётся в отдельной FRT-041;
- добавлен технический `/ui-preview/` для совместной desktop/mobile и keyboard-проверки всех компонентов;
- проверить: открыть `/ui-preview/`, пройти интерактивные элементы клавишей `Tab`, проверить label/error полей, progress, карточку, раму и dialog; на узком viewport не должно быть горизонтального скролла.

### [x] FRT-013. Перенести декоративный слой мастерской

Приоритет: Should  
Версия: Frontend v0.2  
Зависимости: FRT-009, FRT-010

Описание:

- вынести `.studio-props` и фоновые слои в отдельный компонент/стили;
- не включать тяжёлый декоративный слой на страницах, где он не нужен;
- сохранить `aria-hidden`;
- отключать или упрощать декор на узких экранах.

Критерии готовности:

- декоративные элементы не смешаны с семантическим содержимым страницы;
- декор не создаёт горизонтальный скролл и не блокирует взаимодействие.

Выполнено 2026-06-22:

- создан `StudioDecoration.astro` с настенными рамами, полкой красок и прислонёнными холстами; весь компонент имеет `aria-hidden="true"`;
- фоновые слои мастерской вынесены в `src/styles/studio.css`, базовый фон остальных страниц оставлен облегчённым;
- `BaseLayout.astro` включает декор только по явному prop `studioDecoration`; сейчас он активен только на `/`;
- декоративный слой использует `pointer-events: none` и расположен ниже `.site-shell`;
- при ширине до `45rem` объекты и тяжёлые псевдослои полностью отключаются;
- проверить: сравнить `/` и `/artist/`, затем открыть `/` при ширине `390px`; декор должен отсутствовать, горизонтальный скролл — не появляться.

---

## EPIC F4. Маршруты и контентные страницы

### [x] FRT-014. Реализовать главную страницу

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-010, FRT-011, FRT-012, FRT-013

Описание:

- перенести hero;
- перенести карточки входа в три интерактива;
- перенести CTA каталога;
- перенести краткие информационные блоки;
- заменить JS-переходы обычными URL.

Критерии готовности:

- главная визуально сохраняет утверждённую метафору мастерской;
- все CTA ведут на самостоятельные страницы;
- на главной нет скрытых DOM-экранов других разделов.

Выполнено 2026-06-23:

- `src/pages/index.astro` заменён полноценной главной с hero-рамой, музейной цитатой, палитрой и тремя верхними CTA;
- перенесены четыре карточки входа: цвет, детали, свет и каталог; карточки используют общие UI-компоненты и реальные изображения;
- добавлены три кратких информационных блока и ссылка на страницу художника;
- все переходы реализованы обычными ссылками на `/experience/`, `/experience/color-return/`, `/experience/details/`, `/experience/light/`, `/works/` и `/artist/`;
- для ещё не перенесённых интерактивов созданы честные технические страницы-заглушки без скрытых экранов и имитации готовой механики;
- проверить: открыть `/`, пройти все CTA, обновить целевые URL и сравнить desktop/mobile композицию с `docs/VISUAL_BASELINE.md`.

### [x] FRT-015. Реализовать страницу художника

Приоритет: Should  
Версия: Frontend v0.2  
Зависимости: FRT-010, FRT-012

Описание:

- создать самостоятельную страницу биографии и художественного контекста;
- использовать временный структурированный контент до утверждения финального;
- предусмотреть изображения, цитату и связанные серии;
- не смешивать биографию с коммерческой формой.

Критерии готовности:

- страница доступна по `/artist/`;
- её контент можно заменить без изменения layout.

Выполнено 2026-06-23:

- `/artist/` превращён из технической заглушки в самостоятельную редакционную страницу с hero-композицией, изображениями, цитатой, художественным контекстом и временными связанными подборками;
- заменяемый контент и типы вынесены в `src/data/artist-page.ts`, а layout и responsive-стили остаются в `src/pages/artist.astro`;
- временные формулировки и подборки явно маркированы, подтверждённые тексты взяты из legacy-главной, коммерческая форма на странице отсутствует;
- проверить: запустить `npm.cmd run dev`, открыть `/artist/`, проверить desktop/mobile-композицию, изображения и переходы в `/works/` и `/experience/`.

### [x] FRT-016. Реализовать landing интерактивного маршрута

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-010, FRT-012

Описание:

- создать `/experience/`;
- показать порядок интерактивов;
- дать возможность пройти маршрут последовательно или открыть этап напрямую;
- объяснить, что интерактивный опыт отделён от каталога работ.

Критерии готовности:

- пользователь понимает последовательность маршрута;
- каждый этап имеет собственную ссылку.

Выполнено 2026-06-23:

- `/experience/` заменён полноценным landing маршрута с объяснением последовательного и прямого входа;
- три этапа представлены в порядке «Возвращение цвета» → «Приблизить детали» → «Свет в мастерской», каждый ведёт на самостоятельный URL;
- каталог вынесен в отдельный блок и явно не включён в последовательность интерактивного опыта;
- проверить: запустить `npm.cmd run preview`, открыть `/experience/`, пройти ссылки трёх этапов и каталога, затем проверить первый viewport и отсутствие горизонтального scroll на desktop и mobile.

### [x] FRT-017. Добавить 404 и обработку неизвестных slug

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-010

Описание:

- создать оформленную страницу 404;
- предусмотреть поведение при неизвестной работе или серии;
- дать ссылки на главную и каталог.

Критерии готовности:

- неверный URL не показывает пустую или техническую страницу;
- сборка не создаёт ссылки на несуществующие slug.

Выполнено 2026-06-23:

- добавлена оформленная `src/pages/404.astro` в визуальном языке мастерской со ссылками на главную, каталог, интерактивный маршрут и страницу художника;
- неизвестные `/works/<slug>/` и `/series/<slug>/` получают контекстные заголовок и пояснение, оставаясь настоящими HTTP 404;
- production-сборка создаёт `dist/404.html`, а проверка исходников и собранных страниц не находит ссылок на несуществующие slug работ или серий;
- проверить: запустить `npm.cmd run preview`, открыть произвольный неизвестный URL, `/works/missing-work/` и `/series/missing-series/`, затем проверить CTA и HTTP-статус 404.

---

## EPIC F5. Модель данных и подготовка к CMS

### [x] FRT-018. Описать TypeScript-модель произведения

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-002

Описание:

Поддержать поля:

- `id`;
- `slug`;
- `title`;
- `year`;
- `series`;
- `images`;
- `technique`;
- `dimensions`;
- `priceType`;
- `price`;
- `availability`;
- `shortDescription`;
- `fullDescription`;
- `featured`;
- `order`;
- SEO-поля при необходимости.

Критерии готовности:

- карточка и страница работы используют один тип данных;
- `availability` и `priceType` ограничены допустимыми значениями;
- frontend не зависит от названий полей конкретной CMS.

Выполнено 2026-06-23:

- добавлен канонический `Artwork` в `src/types/artwork.ts` со всеми полями FRT-018 и опциональным `seo`;
- `ArtworkSummary` сделан производным от `Artwork` через `Pick<>`, чтобы будущая карточка и страница работы не расходились по контракту;
- `availability` ограничен значениями `available`, `reserved`, `sold`, а `priceType` — `fixed`, `request`; для цены используется нормализованный discriminated union без зависимости от пустых строк legacy/CMS;
- проверить: открыть `src/types/artwork.ts` и выполнить `npm.cmd run check`.

### [x] FRT-019. Создать локальный типизированный источник работ

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-018

Описание:

- перенести `SALES_WORKS` из HTML;
- создать записи для текущих пяти работ;
- добавить slug;
- сохранить временные значения как явно отмеченный контент;
- валидировать записи при сборке.

Критерии готовности:

- каталог строится только из локального источника данных;
- добавление работы не требует изменения компонента каталога;
- ошибочная запись останавливает проверку или сборку с понятным сообщением.

Выполнено:

- создан `src/data/artworks.ts` с пятью текущими записями из legacy `SALES_WORKS`, slug, импортами изображений и сохранёнными временными значениями вроде `Название уточняется`, `Размер уточняется` и демонстрационной серии;
- добавлена build-time/runtime-валидация локальных записей: обязательные поля, уникальные `id`/`slug`/`order`, primary-изображение, корректные `availability`, `priceType` и `price`;
- `/works/` строит основной каталог и архив через `getArtworks()`, поэтому добавление работы не требует изменения страницы каталога;
- проверить: выполнить `npm.cmd run verify`, открыть `/works/` в dev или preview и убедиться, что видны три карточки основного каталога и две архивные работы.

### [x] FRT-020. Описать модель серии

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-018

Описание:

- добавить `id`, `slug`, `title`, `description`, `cover`, `order`;
- связать произведения с сериями по стабильному ID;
- предусмотреть работу без утверждённой серии.

Критерии готовности:

- страницы серий генерируются из данных;
- отсутствующая связь выявляется проверкой.

Выполнено:

- создан `src/types/series.ts` с `SeriesRef`, `SeriesCover`, `Series`, `SeriesSummary` и SEO-полями;
- создан `src/data/series.ts` с временной демонстрационной серией из legacy-каталога, cover-изображением, getter-функциями и валидацией обязательных полей, slug и уникальности `id`/`slug`/`order`;
- `ArtworkSeriesRef` теперь связан с `SeriesRef`, а `src/data/artworks.ts` берёт ссылку серии через стабильный ID и валидирует, что `id`, `slug` и `title` существуют и совпадают с `src/data/series.ts`;
- добавлен минимальный generated route `src/pages/series/[slug].astro`, который строит страницы серий из `getSeries()` и показывает связанные работы через `getArtworksBySeriesId()`;
- проверить: выполнить `npm.cmd run verify`, затем открыть `/series/demo-series-needs-title/` в dev или preview.

### [x] FRT-021. Описать модели narrative и hotspots

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-002

Описание:

- вынести `NARRATIVE_SEQUENCES`;
- вынести `HOTSPOTS`;
- определить TypeScript-типы с ID, порядком, изображением и текстовыми полями;
- отделить текстовое содержание от логики переключения.

Критерии готовности:

- narrative-слайд и hotspot можно изменить без редактирования интерактивного движка;
- некорректная ссылка на следующий этап выявляется проверкой.

Выполнено:

- созданы `src/types/narrative.ts` и `src/types/hotspot.ts` с ID, `order`, изображениями и текстовыми полями;
- legacy `NARRATIVE_SEQUENCES` перенесён в `src/data/narrative.ts` как типизированный массив sequence/slide-записей с сохранёнными временными текстами и getter-функциями;
- legacy `HOTSPOTS` перенесён в `src/data/hotspots.ts` как типизированный массив точек с координатами, `targetScale`, текстами и общей привязкой к изображению интерактива деталей;
- добавлена проверка обязательных полей, уникальности ID/order, координат hotspot и допустимых `completionAction`; некорректный следующий этап ловится типами и валидацией;
- `/experience/` и `/experience/details/` импортируют новые data-модули, чтобы сборка проверяла вынесенный контент без переноса интерактивного движка;
- проверить: выполнить `npm.cmd run verify`, открыть `/experience/` и `/experience/details/`, убедиться, что технические страницы показывают количество narrative-блоков и hotspot-точек.

### [x] FRT-022. Создать frontend-репозиторий данных

Приоритет: Should  
Версия: Frontend v0.3  
Зависимости: FRT-019, FRT-020, FRT-021

Описание:

- определить функции `getArtworks`, `getArtworkBySlug`, `getSeries`, `getNarrativeSequence`;
- не обращаться к файлам данных непосредственно из UI-компонентов;
- вернуть типизированные данные;
- заложить возможность заменить реализацию на Strapi API.

Критерии готовности:

- UI не знает, загружены данные из локальных файлов или CMS;
- будущая интеграция не требует менять props карточек и страниц.

Выполнено:

- создан `src/data/repository.ts` с интерфейсом `FrontendDataRepository`, local-адаптером и typed getter-функциями для artist page, works, series, narrative и hotspots;
- репозиторий экспортирует `getArtworks`, `getArtworkBySlug`, `getSeries`, `getNarrativeSequence` и дополнительные уже используемые getter-функции без раскрытия конкретных local-модулей UI-слою;
- страницы `/artist/`, `/works/`, `/series/[slug]/`, `/experience/` и `/experience/details/` переведены на импорт из `src/data/repository.ts`;
- local-файлы `artist-page.ts`, `artworks.ts`, `series.ts`, `narrative.ts` и `hotspots.ts` остаются заменяемыми источниками под репозиторием, чтобы будущий Strapi/build-time адаптер менялся без изменения props страниц;
- проверить: выполнить `npm.cmd run verify`, затем `rg -n "data/" src\\pages src\\components src\\layouts` и убедиться, что UI импортирует только `data/repository`.

---

## EPIC F6. Каталог, работы и серии

### [x] FRT-023. Реализовать каталог работ

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-012, FRT-019, FRT-022

Описание:

- создать `/works/`;
- отделить доступные и зарезервированные работы от проданных;
- сохранить статусы и два типа цены;
- использовать ссылки на страницы работ;
- не строить основные карточки через runtime `innerHTML`.

Критерии готовности:

- каталог генерируется при сборке;
- карточки имеют осмысленную семантику;
- проданная работа не получает CTA покупки.

Выполнено:

- `/works/` использует `getArtworks()` из frontend-репозитория, разделяет доступные/зарезервированные работы и архив проданных при статической сборке;
- добавлен `src/components/works/ArtworkCatalogCard.astro`: карточка строится Astro-разметкой, содержит `article`, заголовок-ссылку на `/works/{slug}/`, статус, год, технику, размер и два типа цены;
- проданные работы получают архивный CTA `Смотреть в архиве`, а не CTA покупки; доступные и price-on-request карточки сохраняют покупательский/уточняющий CTA;
- проверить: выполнить `npm.cmd run verify`, открыть `/works/` в dev или preview, убедиться, что видны три карточки основного каталога и две архивные карточки, а ссылки карточек ведут на `/works/{slug}/` для будущей FRT-024.

### [x] FRT-024. Реализовать страницу отдельной работы

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-019, FRT-023

Описание:

- создать маршрут `/works/[slug]/`;
- показывать изображение, параметры, статус, цену и описание;
- добавить переход к серии;
- добавить frontend-CTA заявки;
- предусмотреть галерею из нескольких изображений.

Критерии готовности:

- каждая работа имеет самостоятельный URL;
- страница формируется из той же записи, что и карточка каталога;
- прямой вход по URL работает.

Выполнено:

- создан generated route `src/pages/works/[slug].astro`, который строит URL каждой работы из `getArtworks()` и использует ту же запись данных, что и карточка каталога;
- страница показывает основное изображение, параметры, статус, два типа цены, полное описание, ссылку на серию и галерею, готовую к нескольким изображениям;
- добавлен frontend-CTA заявочного сценария без backend-отправки и онлайн-оплаты; проданная работа получает CTA про похожие работы, а не прямую покупку;
- проверить: выполнить `npm.cmd run verify`, открыть `/works/dor-3518/` и `/works/dsc-8599/` в dev или preview, убедиться, что прямой вход работает, статусы/цены совпадают с каталогом, ссылка серии ведёт на `/series/demo-series-needs-title/`.

### [x] FRT-025. Реализовать страницы серий

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-020, FRT-023

Описание:

- создать `/series/[slug]/`;
- вывести описание серии;
- вывести связанные работы;
- добавить обратную ссылку в каталог;
- корректно обрабатывать пустую серию.

Критерии готовности:

- серия не является захардкоженной секцией каталога;
- добавление серии в данные создаёт отдельную страницу.

Выполнено:

- `src/pages/series/[slug].astro` превращён из технической проверки связей в полноценную generated-страницу серии из `getSeries()`;
- страница выводит описание серии, обложку или fallback-блок, счётчики работ, список связанных работ через `getArtworksBySeriesId(series.id)` и карточки `ArtworkCatalogCard`;
- пустая серия обрабатывается через `MessageState` без падения страницы и без хардкода секции каталога;
- проверить: выполнить `npm.cmd run verify`, открыть `/series/demo-series-needs-title/`, убедиться, что видны описание, обложка, пять связанных работ со ссылками на `/works/{slug}/` и обратная ссылка в `/works/`.

### [x] FRT-026. Реализовать архив проданных работ

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-019, FRT-023

Описание:

- создать `/archive/`;
- выводить только проданные работы;
- отделить архив от доступного каталога;
- использовать CTA интереса к похожим работам.

Критерии готовности:

- изменение статуса работы меняет её раздел без изменения компонентов;
- архив доступен отдельной ссылкой.

Выполнено:

- добавлен отдельный маршрут `src/pages/archive/index.astro`, который получает только проданные работы через `getArchivedArtworks()` и показывает CTA интереса к похожим работам;
- `/works/` теперь использует `getCatalogArtworks()` и не смешивает доступные/зарезервированные работы с архивом; `/archive/` доступен из основной навигации и каталога;
- статусные выборки добавлены в `src/data/artworks.ts` и frontend-репозиторий, поэтому изменение `availability` переносит работу между разделами без изменения карточек;
- проверить: выполнить `npm.cmd run verify`, открыть `/works/`, перейти по ссылке «Архив», убедиться, что в каталоге три непроданные работы, в архиве две проданные, а CTA архивной карточки ведёт к секции интереса на `/works/{slug}/#work-inquiry`.

### [x] FRT-027. Реализовать frontend-форму интереса

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-012, FRT-019

Описание:

- сохранить поля текущего прототипа;
- поддержать автоматическую подстановку выбранной работы;
- реализовать клиентскую валидацию;
- явно сообщать, что данные в технической версии не отправляются;
- выделить интерфейс отправки через отдельный адаптер;
- не имитировать сообщение «заявка принята».

Критерии готовности:

- форма имеет состояния `idle`, `validation error`, `prepared`;
- будущий API можно подключить заменой адаптера;
- без backend пользователь не получает ложного подтверждения отправки.

Выполнено:

- добавлен переиспользуемый `src/components/works/InterestForm.astro` с полями legacy-прототипа, доступными ошибками и состояниями `idle`, `validation-error`, `prepared`;
- `src/scripts/interest-form.ts` валидирует имя, контакт, выбранную работу и согласие, подставляет работу из `?work={id}` и переводит форму обратно в `idle` после редактирования или reset;
- `src/utils/inquiry-adapter.ts` отделяет интерфейс подготовки заявки от UI; текущий frontend-only адаптер ничего не отправляет и явно сообщает об отсутствии backend;
- форма подключена к `/works/` и `/works/[slug]/`: CTA карточки передаёт выбранную работу, detail-страница выбирает её напрямую, а sold-сценарий предлагает запрос похожих работ;
- проверить: выполнить `npm.cmd run verify`, открыть `/works/?work=dor-3571#work-inquiry`, отправить пустую форму и увидеть `validation-error`, затем заполнить имя, один контакт, согласие и получить `prepared` с сообщением «Данные не отправлены»; отдельно открыть `/works/dsc-8599/#work-inquiry` и проверить выбранную sold-работу и комментарий о похожих произведениях.

---

## EPIC F7. Narrative-маршрут

### [x] FRT-028. Реализовать универсальный narrative-компонент

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-012, FRT-021

Описание:

- вывести изображение, подпись, основной и дополнительный текст;
- поддержать метаданные;
- поддержать счётчик шагов;
- реализовать `Назад`, `Далее`, `Пропустить`;
- не хранить sequence content внутри компонента.

Критерии готовности:

- компонент работает с последовательностями разной длины;
- кнопки корректно меняются на первом и последнем шагах;
- управление доступно с клавиатуры.

Выполнено:

- добавлен `src/components/narrative/NarrativeSequence.astro`: компонент получает типизированную последовательность через props и выводит метаданные, изображение с подписью, основной и дополнительный текст, счётчик и действия;
- `src/scripts/narrative-sequence.ts` изолирует переключение слайдов, меняет состояния первого и последнего шагов, поддерживает стрелки клавиатуры и отправляет события `narrative:slidechange`, `narrative:complete`, `narrative:skip` без знания о будущих URL-переходах;
- на `/ui-preview/` подключены последовательности `intro` из трёх окон и `lightBridge` из двух окон; контент остаётся в `src/data/narrative.ts`, а не внутри компонента;
- проверить: выполнить `npm.cmd run verify`, открыть `/ui-preview/#preview-narrative-title`, пройти оба блока кнопками и стрелками; на первом шаге `Назад` недоступна, на последнем `Далее` получает `completionLabel` текущего блока.

### [x] FRT-029. Спроектировать URL-переходы narrative-маршрута

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-016, FRT-028

Описание:

- определить, где narrative является частью страницы, а где отдельным шагом;
- не превращать каждый слайд в обязательный самостоятельный URL;
- сохранить возможность прямого входа в каждый интерактив;
- обеспечить понятные переходы после завершения этапа.

Критерии готовности:

- browser Back не возвращает пользователя в случайное скрытое состояние;
- маршрут можно пройти последовательно;
- прямой вход не требует предварительно созданного глобального состояния.

Выполнено:

- в `docs/NARRATIVE_ROUTE_CONTRACT.md` зафиксировано решение: каждый narrative-блок получает один отдельный URL, а слайды остаются локальным состоянием компонента без собственных history entries;
- `src/data/narrative-routes.ts` задаёт валидируемую карту `intro → color-return → bridge → details → lightBridge → light → finale → /`, сохраняя прямые URL трёх интерактивов;
- вход в narrative использует обычный `push`, а завершение и пропуск — `replace` на `completionPath`, поэтому browser Back возвращает к предыдущему содержательному этапу, а не к скрытому или завершённому слайду;
- контракт доступен через `src/data/repository.ts`, не зависит от глобального runtime-состояния и будет подключён к generated-страницам в FRT-030;
- проверить: выполнить `npm.cmd run verify`, сверить таблицу в `docs/NARRATIVE_ROUTE_CONTRACT.md` и убедиться, что сборка валидирует ровно один route-definition для каждой записи `src/data/narrative.ts`.

### [x] FRT-030. Перенести вступление, переход и финал

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-028, FRT-029

Описание:

- перенести текущие `intro`, `bridge`, `finale`;
- сохранить порядок слайдов;
- связать финальные CTA с маршрутами;
- отметить временные тексты в данных, а не в коде компонента.

Критерии готовности:

- все три последовательности доступны;
- замена текстов не требует правки компонента.

Выполнено:

- добавлен generated route `src/pages/experience/story/[sequence].astro`, который собирает самостоятельные страницы для `intro`, `bridge`, актуального дополнительного перехода `lightBridge` и `finale` из route-contract;
- `src/scripts/narrative-route.ts` связывает события `narrative:complete` и `narrative:skip` с `window.location.replace(completionPath)`, не создавая history entries для отдельных слайдов;
- последовательные CTA на главной и `/experience/` теперь открывают `/experience/story/intro/`, а существующие карточки сохраняют прямые входы в каждый интерактив;
- `contentStatus: "placeholder"` хранится в `src/data/narrative.ts` и явно отмечает временный статус текущих текстов без условий внутри компонента;
- проверить: выполнить `npm.cmd run verify`, открыть `/experience/story/intro/`, пройти три окна кнопками или стрелками и завершить блок в `/experience/color-return/`; отдельно проверить прямые URL `/experience/story/bridge/`, `/experience/story/light-bridge/` и `/experience/story/finale/`.

---

## EPIC F8. Интерактив «Возвращение цвета»

### [x] FRT-031. Изолировать canvas-движок раскрытия

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-005

Описание:

- вынести расчёт canvas, кисти, покрытия и прогресса в отдельный модуль;
- не обращаться к элементам других страниц;
- передавать DOM-контейнер и callbacks через явную инициализацию;
- возвращать функцию уничтожения обработчиков;
- сохранить pointer events.

Критерии готовности:

- модуль можно смонтировать и демонтировать повторно;
- повторная инициализация не удваивает обработчики;
- логика не использует глобальные переменные страницы.

Выполнено:

- добавлен `src/scripts/color-reveal-engine.ts`: фабрика принимает container, два canvas, source image, optional brush и callbacks, инкапсулирует crop, resize, grayscale overlay, кисть, coverage grid, pointer capture, порог `82%` и auto-reveal `2100 ms`;
- контроллер возвращает `resize`, `reset`, `startAutoReveal`, `getProgress`, `destroy`; cleanup отменяет animation frame и timer, отключает `ResizeObserver`, освобождает pointer capture и снимает все обработчики;
- `WeakMap` по container перед повторным mount уничтожает прежний экземпляр, поэтому обработчики не удваиваются; движок не использует page selectors или глобальные переменные страницы;
- `src/types/color-reveal.ts` отделяет DOM/options, callbacks, progress и phase-контракты от реализации;
- отображаемый процент равен фактическому coverage: auto-reveal начинается при первом значении не ниже `82%`, после чего прогресс растёт до `100%` синхронно с полным удалением серого слоя;
- на `/ui-preview/#preview-reveal-title` добавлен технический стенд: pointer-жест меняет прогресс, повторный mount, reset, destroy и mount после destroy проверяются до продуктового подключения FRT-032;
- проверить: выполнить `npm.cmd run verify`, открыть `/ui-preview/#preview-reveal-title`, провести по canvas, нажать «Смонтировать повторно», «Сбросить», «Демонтировать» и снова «Смонтировать повторно»; прогресс должен возвращаться к `0%`, а фазы — `idle → destroyed → idle`.

### [x] FRT-032. Реализовать страницу интерактива раскрытия

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-012, FRT-031

Описание:

- создать `/experience/color-return/`;
- перенести stage, HUD, progress и подсказки;
- сохранить reset;
- сохранить auto-reveal;
- оформить завершение через доступный dialog.

Критерии готовности:

- поведение соответствует legacy-прототипу;
- интерактив корректно работает после resize;
- переход дальше ведёт на следующий маршрут.

Реализовано:

- `/experience/color-return/` заменён с технической заглушки на продуктовую Astro-страницу с canvas-stage, HUD, progress, reset и подсказкой;
- клиентский модуль `src/scripts/color-reveal-page.ts` подключает изолированный движок FRT-031, открывает доступный `DialogShell` после auto-reveal и очищает состояние через reset;
- переход продолжения ведёт на `/experience/story/bridge/`, как в narrative route-contract.

Проверка:

- выполнить `npm.cmd run verify`;
- открыть `/experience/color-return/`, провести по картине до auto-reveal, убедиться, что progress становится `100%` и открывается dialog;
- нажать `Раскрасить ещё раз`, убедиться, что dialog закрыт, progress возвращается к `0%`, а серый слой восстановлен;
- проверить узкий viewport: stage и HUD не должны перекрываться, canvas должен пересчитаться под ширину экрана.

### [ ] FRT-033. Добавить тестируемые функции расчёта прогресса

Приоритет: Should  
Версия: Frontend v0.3  
Зависимости: FRT-031

Описание:

- отделить расчёт coverage grid от DOM;
- проверить порог завершения;
- проверить reset;
- проверить граничные координаты кисти.

Критерии готовности:

- ключевые расчёты покрыты unit-тестами;
- изменение визуальной части не ломает прогресс незаметно.

---

## EPIC F9. Интерактив «Приблизить детали»

### [ ] FRT-034. Изолировать zoom/pan-движок

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-005, FRT-021

Описание:

- вынести scale, position, bounds и pointer state;
- отделить математику от DOM-рендера;
- сохранить управление кнопками, wheel и drag;
- реализовать корректное уничтожение обработчиков.

Критерии готовности:

- модуль не зависит от ID элементов других экранов;
- позиция всегда ограничена viewport;
- reset возвращает изображение в fit-состояние.

### [ ] FRT-035. Реализовать hotspots как данные

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-021, FRT-034

Описание:

- строить точки из типизированных данных;
- хранить просмотренные ID внутри экземпляра интерактива;
- центрировать выбранную точку;
- показывать связанный текст в dialog;
- не использовать строковую сборку непроверенного HTML.

Критерии готовности:

- количество точек не зашито в UI;
- завершение срабатывает после просмотра всех активных точек;
- точка доступна клавиатурой.

### [ ] FRT-036. Реализовать страницу исследования деталей

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-012, FRT-034, FRT-035

Описание:

- создать `/experience/details/`;
- перенести viewport, zoom panel, прогресс и модальные состояния;
- добавить инструкции для мыши, touch и клавиатуры;
- сохранить свободный просмотр после завершения.

Критерии готовности:

- поведение соответствует legacy-прототипу;
- интерактив не создаёт page-level горизонтальную прокрутку;
- переход к следующему этапу работает.

### [ ] FRT-037. Покрыть математику zoom/pan unit-тестами

Приоритет: Should  
Версия: Frontend v0.3  
Зависимости: FRT-034

Описание:

- проверить fit scale;
- проверить min/max scale;
- проверить clamp позиции;
- проверить центрирование hotspot;
- проверить resize.

Критерии готовности:

- математические функции тестируются без браузерного DOM.

---

## EPIC F10. Интерактив света

### [ ] FRT-038. Вынести данные картин и состояний света

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-005

Описание:

- вынести `LIGHT_WORKS`;
- вынести `LIGHT_STATES`;
- типизировать изображения, подписи и визуальные параметры;
- отделить данные от DOM-обработчиков.

Критерии готовности:

- новая картина добавляется без изменения управляющей логики;
- число световых состояний не захардкожено в прогрессе.

### [ ] FRT-039. Реализовать изолированный контроллер света

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-038

Описание:

- управлять активной картиной;
- управлять состоянием range;
- отслеживать просмотренные состояния;
- поддержать reset;
- выдавать событие завершения.

Критерии готовности:

- состояние не разделяется с другими интерактивами;
- переключение работы и reset предсказуемы;
- обработчики снимаются при уничтожении.

### [ ] FRT-040. Реализовать страницу интерактива света

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-012, FRT-039

Описание:

- создать `/experience/light/`;
- перенести изображение, selector, range, note и progress;
- оформить завершение через dialog;
- связать CTA с финальным narrative-блоком и каталогом.

Критерии готовности:

- все текущие картины и состояния доступны;
- интерактив работает на touch-устройствах;
- страница не загружает код других интерактивов.

---

## EPIC F11. Модальные окна и доступность

### [ ] FRT-041. Создать доступный Dialog-компонент

Приоритет: Must  
Версия: Frontend v0.3  
Зависимости: FRT-012

Описание:

- использовать нативный `<dialog>` либо корректную ARIA-реализацию;
- переводить фокус внутрь при открытии;
- удерживать фокус внутри;
- закрывать по `Escape` и явной кнопке;
- возвращать фокус в элемент открытия;
- блокировать взаимодействие с фоном.

Критерии готовности:

- все модальные сценарии используют один механизм;
- управление полностью работает без мыши;
- screen reader получает название и описание.

### [ ] FRT-042. Провести аудит семантики страниц

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-014, FRT-023, FRT-024, FRT-032, FRT-036, FRT-040

Описание:

- проверить landmark-элементы;
- проверить один `h1` на страницу;
- проверить порядок заголовков;
- проверить `alt`;
- проверить labels и errors формы;
- проверить кнопки против ссылок;
- проверить `aria-live` только там, где он необходим.

Критерии готовности:

- автоматический accessibility scan не показывает критических ошибок;
- ручной keyboard-only сценарий проходим.

### [ ] FRT-043. Реализовать reduced motion и альтернативы жестам

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-032, FRT-036, FRT-040

Описание:

- учитывать `prefers-reduced-motion`;
- сократить необязательные transition и auto-animation;
- обеспечить кнопочное управление zoom;
- предусмотреть доступный способ завершить canvas-интерактив;
- не делать hover единственным способом получить информацию.

Критерии готовности:

- основные сценарии доступны без drag, wheel и hover;
- reduced-motion не мешает понять изменение состояния.

---

## EPIC F12. Изображения и производительность

### [ ] FRT-044. Провести инвентаризацию изображений

Приоритет: Must  
Версия: Frontend v0.4

Описание:

- зафиксировать назначение каждого изображения;
- отделить оригиналы от web-версий;
- переименовать файлы с пробелами и неоднозначным регистром;
- определить hero, thumbnail, detail и interactive variants.

Критерии готовности:

- каждый production-ассет используется;
- пути имеют единый регистр и безопасные имена;
- оригиналы не загружаются в браузер без необходимости.

### [ ] FRT-045. Подключить Astro image pipeline

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-002, FRT-044

Описание:

- генерировать современные форматы;
- создавать responsive sizes;
- указывать width/height;
- использовать lazy loading ниже первого viewport;
- задавать приоритет только действительно hero-изображениям.

Критерии готовности:

- каталог не загружает полноразмерные оригиналы для миниатюр;
- layout shift от изображений минимизирован;
- изображения корректно собираются в production.

### [ ] FRT-046. Разделить клиентские bundles интерактивов

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-032, FRT-036, FRT-040

Описание:

- не подключать три движка глобально;
- загружать модуль только на его странице;
- проверить отсутствие дублирующей тяжёлой логики;
- не гидратировать статические компоненты без причины.

Критерии готовности:

- страница каталога не загружает canvas/zoom/light код;
- каждый интерактив имеет собственную клиентскую точку входа.

### [ ] FRT-047. Установить performance-бюджет

Приоритет: Should  
Версия: Frontend v0.4  
Зависимости: FRT-045, FRT-046

Описание:

- определить допустимый вес первой загрузки;
- контролировать LCP-изображение;
- проверить mobile throttling;
- зафиксировать Lighthouse-ориентиры;
- отдельно оценивать страницы каталога и интерактивов.

Критерии готовности:

- бюджет записан и проверяем;
- существенное превышение считается блокирующей регрессией.

---

## EPIC F13. SEO-основа frontend

### [ ] FRT-048. Реализовать компонент метаданных

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-010

Описание:

- title;
- description;
- canonical;
- Open Graph;
- social image;
- robots directive;
- безопасные значения по умолчанию.

Критерии готовности:

- каждая индексируемая страница может задать уникальные метаданные;
- страницы работ получают title и description из данных.

### [ ] FRT-049. Добавить sitemap и robots

Приоритет: Should  
Версия: Frontend v0.4  
Зависимости: FRT-017, FRT-048

Описание:

- генерировать sitemap из маршрутов;
- включить страницы работ и серий;
- исключить технические preview-маршруты при их появлении;
- подготовить `robots.txt` без привязки к финальному домену до отдельного этапа настройки.

Критерии готовности:

- сборка создаёт технически корректные файлы;
- неизвестные или архивные dev-страницы не попадают в sitemap.

### [ ] FRT-050. Добавить структурированные данные произведений

Приоритет: Could  
Версия: Frontend v1.0  
Зависимости: FRT-024, FRT-048

Описание:

- добавить JSON-LD для автора и произведений;
- использовать только подтверждённые поля;
- не публиковать фиктивные даты или характеристики как достоверные.

Критерии готовности:

- structured data проходит проверку синтаксиса;
- данные совпадают с видимым контентом страницы.

---

## EPIC F14. Адаптивность и визуальный QA

### [ ] FRT-051. Определить целевые viewport

Приоритет: Must  
Версия: Frontend v0.4

Описание:

Минимально проверить:

- desktop: `1440x900`;
- compact desktop: `1180x760`;
- tablet portrait: `820x1180`;
- mobile: `390x844`;
- узкий mobile: `360x800`.

Критерии готовности:

- viewport записаны в QA-документе;
- для каждого указаны основные проверяемые страницы.

### [ ] FRT-052. Проверить общую оболочку и контентные страницы

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-014, FRT-015, FRT-023, FRT-024, FRT-025, FRT-026, FRT-051

Описание:

- отсутствие горизонтального scroll;
- читаемые строки и отступы;
- корректные карточки;
- доступные CTA;
- предсказуемая навигация;
- отсутствие перекрытия декором.

Критерии готовности:

- все целевые viewport проходят ручной checklist;
- обнаруженные визуальные ошибки зафиксированы отдельными задачами или исправлены.

### [ ] FRT-053. Проверить три интерактива на целевых viewport

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-032, FRT-036, FRT-040, FRT-051

Описание:

- корректный размер canvas;
- доступность controls;
- отсутствие clipping;
- корректный dialog;
- touch-target не меньше согласованного минимума;
- возврат и повторный запуск.

Критерии готовности:

- каждый интерактив можно завершить на desktop и mobile;
- resize и orientation change не приводят к сломанному состоянию.

### [ ] FRT-054. Выполнить визуальное сравнение с legacy-прототипом

Приоритет: Must  
Версия: Frontend v0.4  
Зависимости: FRT-007, FRT-052, FRT-053

Описание:

- сравнить первый экран;
- сравнить карточки запуска;
- сравнить интерактивные stage;
- сравнить каталог;
- сравнить модальные состояния;
- зафиксировать намеренные отличия.

Критерии готовности:

- нет необъяснённых визуальных регрессий;
- новая версия сохраняет художественное направление, не копируя технические ограничения монолита.

---

## EPIC F15. Автоматические проверки

### [ ] FRT-055. Добавить unit-тестовый контур

Приоритет: Should  
Версия: Frontend v0.3  
Зависимости: FRT-002

Описание:

- подключить Vitest или эквивалент;
- тестировать чистые функции;
- не пытаться unit-тестами заменить browser QA;
- добавить тесты схем данных и интерактивной математики.

Критерии готовности:

- `npm.cmd run test` выполняется локально;
- тесты детерминированы;
- минимум покрыты данные, progress и zoom/pan bounds.

### [ ] FRT-056. Добавить browser smoke-тесты

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-014, FRT-023, FRT-032, FRT-036, FRT-040

Описание:

- открытие главной;
- переход в каталог;
- открытие страницы работы;
- запуск каждого интерактива;
- открытие и закрытие dialog;
- frontend-валидация формы;
- проверка отсутствия runtime console errors.

Критерии готовности:

- smoke-набор запускается одной командой;
- тест не зависит от backend и внешних сервисов.

### [ ] FRT-057. Добавить проверки контентных связей

Приоритет: Must  
Версия: Frontend v0.2  
Зависимости: FRT-019, FRT-020, FRT-021

Описание:

- уникальность ID и slug;
- существование изображений;
- существование series ID;
- допустимые статусы;
- корректность narrative next route;
- уникальность hotspot ID.

Критерии готовности:

- некорректные данные не попадают в production-сборку;
- сообщение содержит запись и проблемное поле.

### [ ] FRT-058. Собрать единую release-проверку frontend

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-003, FRT-055, FRT-056, FRT-057

Описание:

- объединить typecheck;
- lint;
- content checks;
- unit tests;
- production build;
- smoke tests.

Критерии готовности:

```powershell
npm.cmd run verify
```

завершается успешно на чистой рабочей копии.

---

## EPIC F16. Документация и завершение миграции

### [ ] FRT-059. Обновить README под новую структуру

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-003, FRT-058

Описание:

- требования окружения;
- установка;
- dev;
- build;
- preview;
- verify;
- карта директорий;
- ссылка на контентную инструкцию;
- ссылка на QA-checklist.

Критерии готовности:

- новый разработчик запускает проект только по README;
- README больше не называет single-file HTML основной версией.

### [ ] FRT-060. Подготовить инструкцию по локальному контенту

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-019, FRT-020, FRT-021, FRT-057

Описание:

- добавление работы;
- изменение статуса и цены;
- добавление серии;
- изменение narrative;
- добавление hotspot;
- требования к изображениям;
- команды проверки.

Критерии готовности:

- контент можно обновить без изучения компонентов;
- инструкция явно временная до подключения CMS.

### [ ] FRT-061. Подготовить ручной QA-checklist

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-051, FRT-056

Описание:

- маршруты;
- каталог;
- карточка работы;
- narrative;
- три интерактива;
- dialogs;
- форма;
- keyboard navigation;
- viewport;
- console;
- production preview.

Критерии готовности:

- checklist можно пройти последовательно;
- для каждого шага указан ожидаемый результат.

### [ ] FRT-062. Переключить основную точку входа

Приоритет: Must  
Версия: Frontend v1.0  
Зависимости: FRT-054, FRT-058, FRT-059, FRT-060, FRT-061

Описание:

- объявить Astro-версию основной;
- переместить `index_masterskaya.html` в `archive/`;
- проверить документацию на устаревшие пути;
- не удалять исторические эксперименты без отдельной задачи;
- зафиксировать дату завершения миграции.

Критерии готовности:

- корневой workflow проекта ведёт в Astro;
- legacy-файл явно обозначен как архив;
- production-сборка не зависит от legacy HTML.

---

## 9. Рекомендуемый порядок выполнения

Не следует параллельно переносить все экраны. Рекомендуется завершать вертикальные этапы:

1. `FRT-001` — `FRT-008`: каркас и эталон.
2. `FRT-009` — `FRT-014`: дизайн-система и новая главная.
3. `FRT-018` — `FRT-027`: данные, каталог, работы, серии и форма-заглушка.
4. `FRT-028` — `FRT-030`: narrative-маршрут.
5. `FRT-031` — `FRT-033`: «Возвращение цвета».
6. `FRT-034` — `FRT-037`: «Приблизить детали».
7. `FRT-038` — `FRT-040`: интерактив света.
8. `FRT-041` — `FRT-050`: доступность, изображения и SEO.
9. `FRT-051` — `FRT-058`: визуальный QA и автоматические проверки.
10. `FRT-059` — `FRT-062`: документация и переключение точки входа.

После завершения каждого вертикального этапа:

```powershell
npm.cmd run verify
npm.cmd run dev
```

Затем вручную проверяется изменённый пользовательский сценарий и сравнивается с legacy-прототипом.

---

## 10. Критерии готовности Frontend v1.0

Frontend-переработка считается завершённой, если:

- основной проект запускается и собирается через npm-команды;
- `index_masterskaya.html` больше не является основной реализацией;
- главная, художник, каталог, архив, серии, работы и интерактивы имеют отдельные URL;
- обновление любой страницы не ломает её состояние;
- каталог, narrative и hotspots находятся в типизированных данных;
- страницы и карточки не дублируют модель произведения;
- три интерактива изолированы и не используют общее глобальное состояние;
- код интерактивов загружается только на соответствующих страницах;
- форма честно работает как frontend-заглушка и готова к подключению API;
- изображения проходят оптимизирующий pipeline;
- основные маршруты работают на целевых viewport;
- все dialogs и основные действия доступны с клавиатуры;
- отсутствуют критические runtime, TypeScript, lint и accessibility ошибки;
- `npm.cmd run verify` выполняется успешно;
- README, инструкция по контенту и QA-checklist отражают текущую реализацию.

---

## 11. Следующий этап после этого бэклога

После завершения Frontend v1.0 можно отдельным решением выбирать и подключать:

- Strapi или другую CMS;
- способ динамической загрузки либо пересборки контента;
- серверную обработку заявок;
- базу данных;
- хостинг и deployment pipeline.

Эти работы не должны требовать повторного проектирования frontend. Они подключаются через заранее выделенный слой данных и адаптер формы.
